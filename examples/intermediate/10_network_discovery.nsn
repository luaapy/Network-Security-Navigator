# NSN Example: Network Discovery

subnet = "192.168.1.0/24"
print "Discovering hosts on " + subnet

hosts = discover subnet

print "Discovery complete. Found " + len(hosts) + " hosts:"
for host in hosts {
    print " - " + host
}